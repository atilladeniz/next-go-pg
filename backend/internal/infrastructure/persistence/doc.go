// Package persistence holds concrete implementations of the repository
// ports declared in internal/application. GORM-tagged models and mappers
// between domain types and persistence models live here. The domain
// package must never import this package.
package persistence
